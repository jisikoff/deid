package com.graticule.deid.hash

enum class ElementType {
    FIRST_NAME,
    FIRST_3_FIRST_NAME,
    SOUNDEX_FIRST_NAME,
    LAST_NAME,
    FIRST_3_LAST_NAME,
    SOUNDEX_LAST_NAME,
    BIRTH_YEAR,
    BIRTH_DAY,
    BIRTH_MONTH,
    GENDER,
    SSN
}